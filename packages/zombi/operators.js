/*
    This is the "operators" entry-point, exported separately from the main
    bundle so they can be imported like:

      `import { sideEffect } from 'zombi/operators';`
 */

module.exports = require('./dist/operators');
